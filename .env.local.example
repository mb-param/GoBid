# Copy to `.env.local` for local dev (`npm run dev` inside operator-console/).
NEXT_PUBLIC_OPS_API_ORIGIN=http://127.0.0.1:3800
# Mirrors tender-platform OPS_API_TOKEN for browser/EventSource (?token=)
NEXT_PUBLIC_OPS_API_TOKEN=
